import {Coordinate} from "./coordinate";
import {Box} from "./box";
import {colour, width} from "./enums";

describe('1. ', () => {
    describe("1.1 ", () => {
        it('should throw an error', () => {
            let box = new Box (new Coordinate(1, width.a), colour.BLACK);
          })
    })
  });
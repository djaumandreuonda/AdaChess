enum colour {
    WHITE, BLACK, GREEN, RED, YELLOW
}
enum x {
    a, b, c, d, e, f, g, h
}
enum type {
    king = "king",
    rook = "rook",
    bishop ="bishop",
    queen = "queen",
    knight = "knigth",
    pawn = "pawn",
}

export{ colour, x, type }